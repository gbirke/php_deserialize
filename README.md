php_deserialize is a shell script to display files containing serialized PHP data.

Usage: `php_deserialize filename`

Planned improvements:
  - Read from stdin
  - Multiple filenames
  - Option to use `var_dump` instead of `var_export`


